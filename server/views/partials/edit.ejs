<!-- Main Nav-->
<nav class="navbar navbar-expand-lg sticky-top"style="background-color: #840712;">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src="../../Assets/images/pancakelogo.png" alt="Avatar Logo" style="width:80px;" class="bg-transparent"> 
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
      
        <!-- Nav link for Home page-->
        <li class="nav-item text-dark">
          <% if (title == "Home") {%>
          <a class="nav-link active" aria-current="page" href="/home">
          <%} else { %>
          <a class="nav-link " aria-current="page" href="/home">
          <%} %>
          <i class="fa-brands fa-fort-awesome-alt"></i>Home</a>
        </li>
        
      <!-- Nav link for Apperals page-->
        <li class="nav-item text-dark">
          <% if (title == "Apparels") {%>
          <a class="nav-link active" aria-current="page" href="/apparelList">
          <%} else { %>
          <a class="nav-link " aria-current="page" href="/apparelList">
          <%} %>
          <i class="fa-solid fa-hat-wizard"></i>Apparels</a>
        </li>
        
        <!-- Nav link for Display page-->
        <li class="nav-item">
        <% if (title  == "Display") { %>
          <a class="nav-link active "  href="/display">
        <% } else {%>
        <a class="nav-link"  href="/display">
        <% } %>
          <i class="fa-brands fa-phoenix-framework"></i> Fashion Trends</a>
        </li>

        <!-- nav for users wishlist. only shows if user is logged in-->
        <!-- Nav link for Contact page-->
       <li class="nav-item">
        <% if (title  == "Contact Me") { %>
          <a class="nav-link active "  href="/contact">
        <% } else {%>
        <a class="nav-link"  href="/contact">
        <% } %>
          <i class="fa-solid fa-file-signature"></i> Contact</a>
        </li>

  <!-- <% if(typeof displayName =='string' && displayName) { %>
          <li class="nav-item">
          <% if (title  == "Wish") { %>
            <a class="nav-link active "  href="/wishList">
          <% } else {%>
          <a class="nav-link"  href="/wishList">
          <% } %>
          <i class="fa-brands fa-phoenix-squadron"></i>Exclusive WishList</a>
        </li>  
      <% } %> -->

      <!-- nav for  login & logout-->
      <% if(typeof displayName =='string' && displayName) { %>
      <li class="nav-item">
      <div class="dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" >
        <i class="fas fa-lg fa-user"></i> Welcome: <%= displayName %>
        </button>
        <!-- Drop Down Items that only users logged in can see-->
        <ul class="dropdown-menu dropdown-menu-dark">
          <!-- For Members Exclusive Wishlist -->
          <li>
          <a class="dropdown-item"  href="/wishList">
          <i class="fa-brands fa-phoenix-squadron"></i>Exclusive WishList</a>
          </li> 
          <!-- For User Account Details -->
          <li>
          <a class="dropdown-item"data-bs-toggle="modal" data-bs-target="#exampleModal">Account Details</a>
          </li>
         
          <li>
          <a class="dropdown-item" href="/logout" >
          <i class="fas fa-lg fa-sign-out-alt"></i>Logout</a></li>
        </ul>
      </div>
      </li>
      <% } else { %>
        <li class="nav-item">
          <a href="/login" class="nav-link">
          <i class="fas fa-lg fa-sign-in-alt"></i>Login</a></li>
      <% } %>
        
      </ul>
    </div>
  </div>
</nav>
